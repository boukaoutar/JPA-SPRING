package com.example.assoc.dao;

import org.springframework.data.jpa.repository.JpaRepository;

import com.example.assoc.entities.Cible;

public interface CibleRepository extends JpaRepository<Cible, Integer>{

}
